<% @sites.each do |r| %>
<%= r.id %>
 <%= r.subtype %>  <%= r.siteStreet %>
  <%= r.siteCrossStreets %> 
   <%= r.siteCounty %>
   <%= r.siteZip %>
   <%= r.cityWardNumber %>
   <%= r.countyDistrict %>
        
    
  <% end %>
 