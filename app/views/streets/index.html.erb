<%= render 'shared/page_title', title: "All Streets" %>
<ul class="nav nav-tabs">
    <li>
		<%= link_to "All Sites", sites_path %>
	</li>
	<li class="active">
		<%= link_to "Streets", streets_path %>
	</li>
		<li>
		<%= link_to "Parks", parks_path %>
	</li>	
	<li>
		<%= link_to "Washs", washes_path %>
	</li>

</ul>

<%= form_tag(streets_path, :method => "get", id: "search-form") do %>
<%= text_field_tag :search, params[:search], placeholder: "Search Streets" %>
<%= submit_tag "Search" %>
<% end %>

<% if @streets.present? %>
<div class="table-container">
	<table class="data-table">
		<thead>
			<tr>
				<th>Site Street</th>
				<th>Cross-Streets</th>
				<th>County</th>
				<th>Zip Code</th>
				<th>City Ward Number</th>
				<th>County District</th>

			</tr>
		</thead>
  <%= render @streets %>
  	</table>
</div>
<% else %>
  <p>There are no Streets containing the information <%= params[:search] %> exist.</p>
<% end %>
